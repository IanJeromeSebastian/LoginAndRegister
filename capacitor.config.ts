import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firstapp-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
